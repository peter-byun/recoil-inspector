import React from 'react';
import ReactDOM from 'react-dom/client';
import Frontend from './Frontend';

ReactDOM.createRoot(document.getElementById('frontend-root')).render(
  <React.StrictMode>
    <Frontend />
  </React.StrictMode>
);
