name: Publish Package to npmjs
on:
  release:
    types: [published]
jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup NVM
        run: |
          npm install -g nvm
          nvm install

      - name: Setup yarn
        run: npm install -g yarn

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install

      - name: Build the package
        run: yarn run build --filter recoil-inspector

      - name: Change directory to package
        run: cd packages/recoil-inspector

      - name: Bump version
        run: npm version patch

      - name: Publish package
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Commit and push changes
        run: |
          git config --local user.email "peter1035k@gmail.com"
          git config --local user.name "Peter Byun"
          git add -A
          git commit -m "chore: updated package versions"
          git push origin main
